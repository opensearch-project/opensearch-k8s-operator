name: Golang Lint
on: [pull_request]

jobs:
  golangci:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - name: golangci-lint
        run: |
          cd opensearch-operator
          go get .
          golangci-lint run